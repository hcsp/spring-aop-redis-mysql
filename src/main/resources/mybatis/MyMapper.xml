<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultMap="rankItem">
        select goods.name as goodsName, sum(`order`.price*`order`.quantity) as score from goods
        left join `order`
        on `order`.goods_id=goods.id
        group by goods.id
        order by score desc;
    </select>
    <resultMap id="rankItem" type="com.github.hcsp.entity.RankItem">
        <result property="score" column="score"/>
        <association property="goods" javaType="com.github.hcsp.entity.Goods">
            <result property="name" column="goodsName"/>
        </association>
    </resultMap>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultMap="rankItem">
        select g.name as goods_name, amount
        from (select goods_id, sum(price * quantity) as amount
              from `order`
              group by goods_id) t
                 left join goods g
                            on t.goods_id = g.id
        order by amount desc
    </select>

    <resultMap id="rankItem" type="com.github.hcsp.entity.RankItem">
        <result property="amount" column="amount"/>
        <association property="good" javaType="com.github.hcsp.entity.Good">
            <result property="name" column="goods_name"/>
        </association>
    </resultMap>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectGoodsRank" resultMap="GoodsRank">
        select goods.id as goods_id, goods.name as goods_name, t.total_price
        from (select goods_id, sum(price * quantity) as total_price
              from `order`
              group by goods_id) t
                 right join goods on t.goods_id = goods.id
        order by t.total_price desc;
    </select>
    <resultMap id="GoodsRank" type="Rank">
        <result property="totalPrice" column="total_price"/>
        <association property="goods" javaType="Goods">
            <result property="id" column="goods_id"/>
            <result property="name" column="goods_name"/>
        </association>
    </resultMap>
</mapper>

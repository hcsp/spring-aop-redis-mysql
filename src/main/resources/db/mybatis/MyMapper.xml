<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultMap="rankItem">
        select t.goods_id,goods.name as goods_name,t.sales
        from
        (select goods_id,sum(price * quantity) as sales
            from `order` group by goods_id) t
        right join goods
        on t.goods_id = goods.id
        order by t.sales desc
    </select>
    <resultMap id="rankItem" type="com.github.hcsp.entity.RankItem">
        <result property="sales" column="sales"/>
        <association property="goods" javaType="com.github.hcsp.entity.Goods">
            <result property="id" column="goods_id"/>
            <result property="name" column="goods_name"/>
        </association>
    </resultMap>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultMap="rankItem">
        select goods.name as goods_name, o.price_sum
        from (
        select goods_id, sum(price * quantity) as price_sum
        from `order`
        group by goods_id
        ) o
        inner join goods on o.goods_id = goods.id
        order by price_sum desc;
    </select>

    <resultMap id="rankItem" type="com.github.hcsp.entity.RankItem">
        <result property="name" column="goods_name"/>
        <result property="gmv" column="price_sum"/>
    </resultMap>

</mapper>
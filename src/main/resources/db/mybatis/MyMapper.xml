<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultType="com.github.hcsp.entity.RankItem">
        select goods.name as name, ifnull(t.total_price,0) as total_price
        from goods
                 left join (select goods_id, sum(price * quantity) total_price from `order` group by goods_id) t
                      on goods.id = t.goods_id
        order by total_price desc;
    </select>
<!--    <resultMap id="" type=""><result /></resultMap>-->
</mapper>
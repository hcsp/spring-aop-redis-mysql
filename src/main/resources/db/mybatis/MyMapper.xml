<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="selectRank" resultMap="rankItem">
        select goods.name as goods_name,t.trading_volume
        from
        (
        select goods_id,sum(price*quantity) as trading_volume
        from `order` group by goods_id
        ) t
        inner join goods
        on goods.id = t.goods_id
        order by trading_volume desc
    </select>

    <resultMap id="rankItem" type="com.github.hcsp.entity.RankItem">
        <result property="price" column="trading_volume"/>
        <association property="goods" javaType="com.github.hcsp.entity.Goods">
            <result property="name" column="goods_name"/>
        </association>
    </resultMap>
</mapper>